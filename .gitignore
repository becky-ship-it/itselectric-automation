# Credentials — never commit
token.json
credentials.json

# Local pipeline config (may contain spreadsheet ID)
config.yaml

# Claude context — local only
CLAUDE.md

# Python
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
.eggs/

# Tools
.ruff_cache/
.pytest_cache/
.coverage

# PyInstaller / build output
build/
dist/
venv/
*.spec
*.spec.bak

# macOS
.DS_Store

# Git worktrees
.worktrees/

# SQLite database
data/*.db

# Geocache (seeded into DB on first run; runtime data)
geocache.json

# Dev tool artifacts
.playwright-mcp/

# Local plan docs
docs/plans/
